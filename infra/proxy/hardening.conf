# Loaded by nginx-proxy from conf.d (mounted in docker-compose.yml). WordPress adds the
# per-response security headers itself (infra/wp/mu-plugins/fleet-security.php); the edge just
# stops leaking its own version and refuses oversized / slow-loris style requests.
server_tokens off;
client_body_timeout 30s;
client_header_timeout 30s;
