anthropic>=0.60
feedparser>=6.0
requests>=2.32
trafilatura>=1.12
Pillow>=10.3
PyYAML>=6.0
pydantic>=2.7
tweepy>=4.14
python-slugify>=8.0
opencv-python-headless>=4.9   # face-aware cover crops (autopub/images.py)
numpy>=1.26
imageio-ffmpeg>=0.5        # bundled ffmpeg binary for the reels (autopub/video.py)
piper-tts>=1.8             # local neural voice for the reel narration (autopub/speech.py)
kokoro-onnx>=0.6           # the natural-sounding narration voice (autopub/speech.py); piper stays as the fallback
yt-dlp>=2025.1.1           # fetches the ad film itself when repost_ads is on (autopub/adclip.py)
